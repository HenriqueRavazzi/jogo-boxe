"use client";

/** @deprecated Prefer `PostRunSummaryModal` — mantido para imports existentes. */
export {
  PostRunSummaryModal as GameOverModal,
  PostRunSummaryModal,
} from "@/components/PostRunSummaryModal";
